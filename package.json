{
  "name": "cds-common-jwt-support",
  "version": "0.0.2",
  "description": "support common jwt with jose for @sap/cap",
  "main": "lib/index.js",
  "author": {
    "name": "Theo Sun"
  },
  "scripts": {
    "test": "jest --detectOpenHandles --passWithNoTests",
    "prettier": "prettier --check --write src",
    "build": "tsc",
    "postbuild": "tsc --noEmit --p tsconfig.lib.json",
    "lint": "eslint src/** --fix",
    "release": "npx standard-version --no-verify",
    "coverage": "npm test -- --coverage --no-cache"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^22.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "commitlint": "^19.0.0",
    "eslint": "^9.0.0",
    "jest": "^30.0.0",
    "jose": "^6.0.0",
    "ts-jest": "^29",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "CAP",
    "@sap/cds",
    "cds",
    "auth",
    "jwt"
  ],
  "license": "Apache-2.0",
  "dependencies": {
    "cds-internal-tool": "^1.6.5"
  }
}
